============================================================
  Picklog Backend - 프로젝트 진행 상황 요약
  작성일: 2026-05-01
============================================================

1. 프로젝트 개요
------------------------------------------------------------
- 프로젝트명: Picklog (인스타그램 장소 아카이빙 서비스)
- 역할: 백엔드 API 서버
- 기술 스택: FastAPI + SQLAlchemy 2.x + PostgreSQL(PostGIS) + Playwright
- 배포: Railway (API) + Supabase (DB)


2. 인프라 구성 (완료)
------------------------------------------------------------
[로컬 개발 환경]
  - Docker Compose: PostgreSQL 15 + PostGIS 3.3, Redis
  - Poetry 기반 의존성 관리 (Python >=3.14)
  - Alembic 마이그레이션 관리

[클라우드 배포]
  - DB: Supabase (PostgreSQL + PostGIS)
    - Session Pooler 방식 (IPv4 지원)
    - 연결: aws-1-ap-southeast-1.pooler.supabase.com
  - API: Railway
    - GitHub 연동 자동 배포 (main 브랜치 push 시)
    - Dockerfile 기반 빌드
    - 도메인: capstone2be-production.up.railway.app
    - Swagger UI: https://capstone2be-production.up.railway.app/docs


3. DB 스키마 (완료 - 13개 테이블)
------------------------------------------------------------
  instagram_crawl_jobs    - 비동기 크롤링 잡 상태/결과 (id, status, source, payload, error)
  users              - 사용자 (email, password, kakao_id, nickname, profile_image)
  storages           - 저장소/창고 (title, description, is_public, deleted_at)
  storage_members    - 저장소 멤버 (storage_id, user_id, role: owner/editor/viewer)
  places             - 장소 마스터 (name, address, coordinate(PostGIS POINT), category_group, phone, homepage_url)
  place_raw_data     - 1차 raw 데이터 (provider, provider_place_id, raw_payload(JSONB), place_id 'nullable'). 인스타 raw도 여기 통합
  place_reviews      - 장소 리뷰 (네이버 블로그 등 외부 출처)
  place_images       - 장소 이미지 (image_url, source, is_representative)
  spots              - 스팟 (storage_id, place_id, added_by, instagram_url, caption, user_memo, user_rating, is_visited)
  place_space_dna    - 장소 공간DNA (mbti_axes(JSONB), ai_summary)
  tags               - 태그 마스터 (name)
  place_tags         - 장소-태그 연결 (place_id, tag_id, score)
  user_space_dna     - 유저 공간DNA (mbti_axes(JSONB), preferred_vibe_tags, total_visits)
  user_space_dna_history - 유저 DNA 변화 이력 (spot_id, mbti_axes_snapshot)

핵심 설계 패턴:
  - StorageMember 다대다 관계로 공유 창고 지원 (owner/editor/viewer 역할)
  - Place-first 정규화: 장소 정보를 Place에 중앙 관리, Spot은 개인 저장 기록
  - 소프트 삭제: storages.deleted_at, spots.deleted_at
  - GeoAlchemy2 POINT + GIST 인덱스 (idx_places_coordinate 자동 생성)


4. 구현된 API 엔드포인트 (완료)
------------------------------------------------------------
[인증 - /auth]
  POST /auth/register       회원가입 (기본 저장소 자동 생성)
  POST /auth/login          로그인 (OAuth2 form data, JWT 발급)
  POST /auth/kakao          카카오 OAuth 로그인 (모바일 SDK access_token → JWT)

[사용자 - /users]
  GET  /users/me            내 정보 조회
  PUT  /users/me            프로필 수정 (nickname, profile_image)
  GET  /users/me/space-dna  내 공간 DNA 조회 (없으면 has_data=false)
  GET  /users/me/pins?storage_ids=&visited=&bbox=  지도 마커용 슬림 응답 (다중 storage 합치기, viewport bbox·visited 필터, 1000건 캡 + X-Truncated 헤더)
  GET  /users/search?q=&size=  닉네임 prefix 검색 (창고 초대용. 본인 제외, 카카오 임시 이메일 우회)

[저장소 - /storages]
  GET    /storages                   내 저장소 목록 (페이징: page, size)
  POST   /storages                   저장소 생성 (owner 자동 등록)
  GET    /storages/{id}              저장소 상세 조회
  PUT    /storages/{id}              저장소 수정 (owner, editor)
  DELETE /storages/{id}              저장소 삭제 (소프트 삭제, owner만)

[스팟 - /storages/{storage_id}/spots]
  GET    /storages/{id}/spots        스팟 목록 (페이징: page, size)
  POST   /storages/{id}/spots        스팟 생성 (place_id 필수)
  GET    /storages/{id}/spots/{sid}  스팟 상세
  PUT    /storages/{id}/spots/{sid}  스팟 수정 (is_visited=True 시 visited_at 자동 기록)
  DELETE /storages/{id}/spots/{sid}  스팟 삭제 (소프트 삭제)

[창고 멤버 - /storages/{storage_id}/members]
  GET    /storages/{id}/members          멤버 목록 (멤버 누구나, nickname/profile_image 포함)
  POST   /storages/{id}/members          user_id로 멤버 추가 (owner, editor|viewer만)
  PATCH  /storages/{id}/members/{uid}    role 변경 (owner. role="owner"면 기존 owner→editor 자동 강등 atomic)
  DELETE /storages/{id}/members/{uid}    멤버 추방 (owner. 본인 추방은 400, /me 안내)
  DELETE /storages/{id}/members/me       self-leave (모든 멤버. owner는 400 거부)

[장소 - /places]
  GET  /places?q=검색어              장소 검색 (이름 기반, 페이징)
  GET  /places/{id}                  장소 상세 조회
  GET  /places/{id}/space-dna        장소 공간 DNA 조회 (없으면 has_data=false)

[인스타그램 - /instagram]
  POST /instagram/crawl         URL 크롤링 (OG 메타, 동기 - 레거시 유지)
  POST /instagram/crawl-async   Apify 기반 크롤링 (비동기 큐, 캐시 hit 시 즉시 반환)
  GET  /instagram/jobs/{id}     비동기 잡 상태/결과 조회
  POST /instagram/share         자동 매핑 + 저장 (캡션 → 후보 추출 → 네이버 검색 → 유니크 1건이면 자동 저장, 그 외 후보 반환)
  POST /instagram/save          크롤링 + 저장 (Place -> PlaceRawData -> PlaceImage -> Spot, 수동 폴백 경로 포함)

[헬스체크 - /health]
  GET  /health/db            DB 연결 상태 확인


5. 주요 구현 특징
------------------------------------------------------------
- JWT 인증: OAuth2PasswordBearer + Swagger Authorize 버튼 정상 동작
- 권한 관리: StorageMember 기반 role 검사 (owner/editor/viewer)
- Instagram 크롤링: Playwright + BeautifulSoup OG 메타 파싱
  - URL 검증 (instagram.com 여부)
  - 비공개/삭제 게시물 감지 (OG 메타 비어있으면 404)
  - 타임아웃/차단 처리 (504)
- 페이징: GET /storages, GET /spots에 page/size 쿼리 파라미터
- 소프트 삭제: deleted_at 필드 활용, 목록 조회 시 자동 필터


6. 프로젝트 파일 구조
------------------------------------------------------------
app/
  main.py                    앱 진입점, 라우터 등록, Playwright 라이프사이클
  core/
    database.py              SQLAlchemy 엔진 + 세션 팩토리 + get_db()
    security.py              JWT 생성/검증, 비밀번호 해싱
  dependencies/
    auth.py                  get_current_user (OAuth2 Bearer 토큰 → User 객체)
  models/
    models.py                SQLAlchemy ORM 모델 12개 클래스
  schemas/
    user.py                  UserCreate, UserResponse, Token
    storage.py               StorageCreate/Update/Response, StorageMemberResponse
    spot.py                  SpotCreate/Update/Response
    place.py                 PlaceCreate, PlaceResponse
    instagram.py             InstagramCrawlRequest/Response, InstagramSaveRequest
  routers/
    auth.py                  회원가입/로그인
    users.py                 프로필 조회/수정
    storages.py              저장소 CRUD
    spots.py                 스팟 CRUD
    places.py                장소 검색/조회
    instagram.py             인스타그램 크롤링/저장
    health.py                헬스체크
  services/
    instagram_crawler.py     Playwright 기반 크롤링 로직 (OG fallback)
    playwright_manager.py    브라우저 싱글톤 관리
    apify_client.py          Apify 액터 호출 래퍼
    instagram_pipeline.py    캐시 → Apify → OG fallback 통합 파이프라인
    instagram_jobs.py        RQ 잡 함수 (process_crawl_job)
    instagram_share.py       /share 자동 매핑 + 저장 오케스트레이션
    naver_local_search.py    Naver Local Search Open API 래퍼
    place_extractor.py       캡션 → 장소 후보 텍스트 추출 (정규식·휴리스틱)
    spot_creator.py          Place upsert + Spot 생성 공용 로직 (/save · /share 공통)
    naver_blog.py            네이버 블로그 검색 + 본문 추출 (PlaceReview 수집)
    kakao_oauth.py           카카오 OAuth 사용자 정보 조회

Dockerfile                   Railway 배포용 (Playwright Chromium 포함)
docker-compose.yml           로컬 개발용 (PostgreSQL + Redis)
alembic.ini                  Alembic 설정
migrations/                  DB 마이그레이션 파일
pyproject.toml               Poetry 의존성


7. 미구현 항목 (향후 작업)
------------------------------------------------------------
 우선순위  |  항목                              |  비고
----------+------------------------------------+-------------------------------
 1순위    |  창고 멤버 토큰 초대 링크          |  기획 문서 명시 — 현재는 user_id 직접 추가만
 2순위    |  공간 탐색 (반경 검색)              |  PostGIS ST_DWithin, 네이버맵 API 연동 필요
 3순위    |  공개 창고 탐색/피드 + 스크랩      |  is_public 필드 활용
 4순위    |  장소 DNA AI 분석                   |  AI팀 진행 중 (Supabase 직접 write)
 5순위    |  Instagram location_id 인증 추출    |  쿠키 주입 또는 네이버 API 우회 방식 필요
 6순위    |  네이버↔인스타 크로스 중복 병합     |  좌표 근접 매칭 또는 Place 병합 UI 필요
 인프라   |  /tests + ruff lint                 |  자동 회귀 방어 부재가 가장 큰 리스크


8. 최근 수정 사항 (2026-05-22 업데이트)
------------------------------------------------------------
[2026-05-22 — 다중 장소 게시물: 선택된 장소의 이미지만 분류 저장]
- 배경: 인스타 큐레이션 게시물(여러 장소 + 장소별 여러 사진)은 needs_selection으로
  사용자가 1개를 골라도, /save가 캐러셀 전체를 그 Spot에 적재해 다른 장소 사진까지
  같은 갤러리에 섞임. AI 공간 DNA 분석 입력도 다른 장소 사진까지 받음.
- 5중 검증으로 plan 보정
  - 가설 1: Anthropic SDK + messages.parse() + 비전 + 구조화 출력 → 같은 모듈 패턴
    (place_extractor_llm, place_disambiguator)이 이미 messages.parse 사용 중. 비전
    입력은 신규(스모크 1회로 확인).
  - 가설 2: claude-haiku-4-5 비전 지원 → Claude 4.x 패밀리 전체 지원, 기존 모듈이
    동일 모델 사용 중.
  - 가설 3: /save 시점에 캐러셀 전체 이미지 접근 가능? → InstagramCrawlData가
    thumbnail_url만 노출. **빈틈 발견 — image_urls 필드 추가가 본 PR 범위**.
  - 가설 4: PlaceCandidate를 /save로 되돌리기 호환 → 필드들이 1:1 대응, 분류
    컨텍스트로 그대로 재활용 가능.
  - 가설 5: Pillow 신규 의존성 + image_storage 독립성 → grep 0건(신규). image_storage는
    자체 httpx 다운로드·업로드, 분류 모듈과 코드 경로 완전 분리.
- 핵심 변경
  - app/services/image_place_matcher.py (신규): Claude haiku-4-5 vision 분류기.
    이미지 다운로드 → PIL로 512px max dim 리사이즈 → JPEG base64 → messages.parse
    (output_format=ImagePlaceMatchResult). 검증 깨짐/API 실패 모두 graceful
    degradation으로 {0: 전체} 폴백 반환.
  - app/schemas/instagram.py: InstagramSaveRequest.candidates_context 추가
    (PlaceCandidate 리스트, 옵셔널). InstagramCrawlData.image_urls 추가
    (needs_selection 응답에서 클라이언트가 /save로 되돌릴 수 있게). 후방 호환 위해
    model_rebuild() 추가.
  - app/services/instagram_share.py: share_result_to_response가 crawl_data.image_urls
    까지 채움.
  - app/routers/instagram.py: /save에서 candidates_context+image_urls가 둘 다 있고
    각각 2개 이상일 때만 matcher 호출. 선택 장소(naver_place_id)에 배정된 image만
    추려 InstagramData에 전달. 0장이면 첫 이미지를 대표로 강제 배정.
  - pyproject.toml: pillow>=11 추가. 분류용 다운스케일에만 사용, DB 저장은 image_storage
    가 원본 그대로 유지.
- 정책 결정
  - 분류용은 512px 다운스케일, DB 저장은 원본 — 게시물당 ~$0.0075 예상.
  - temperature=0.0 명시 — 2026-05-22 로컬 e2e에서 같은 게시물 두 번 저장 시 슬라이드 1이
    place 0/1로 갈리는 비결정성 관측. 완전 결정성은 Redis 캐시로 후속(별도 PR).
  - /save 시점 지연 호출 — 저장 안 하는 게시물의 비용 낭비 회피.
  - 폴백은 첫 후보에 전체 몰아넣기 — 호출부에서 0장 처리 시 첫 이미지 강제 배정.
  - 단일 자동 저장 케이스는 손대지 않음 (matcher 미호출).
  - 클라이언트가 candidates_context 전달 — Redis 캐시·재추출 대비 단순함, 옵셔널 호환.
- 신규 진단 스크립트 scripts/_oneoff_test_image_matcher.py
  - DB raw_payload 1건의 caption·images로 matcher 호출, 응답시간·비용 추정.
  - read-only.
- 알려진 한계
  - 클라이언트 합류 전엔 분류 미작동(옵셔널 필드라 회귀 없음).
  - prompt caching 미사용 (호출 빈도 늘면 도입).
  - 평가 셋·라벨 없음 — 정성 평가만.
  - 분류용/저장용 다운로드 중복 (데이터량 작아 후순위).

[2026-05-21 — 인스타 캐러셀 전체 이미지 영구 저장]
- 배경: /instagram/share·/instagram/save가 게시물 썸네일 1장만 PlaceImage에 저장.
  AI팀 공간 DNA 분석이 단일 이미지(특히 자막+음식 첫 컷)에 좌우돼 결과 편향.
  캐러셀 전체 슬라이드를 영구 저장해 분석 입력 풀을 확보.
- 4중 검증으로 plan 보정
  - 가설 1: Apify가 캐러셀을 어떤 키로 반환? → 핸드오프 문서 3곳(ai-team-handoff,
    space-dna-auto-trigger, seeds/README)이 일관되게 raw_payload['images'] 평탄
    배열 패턴 명시 → _normalize_apify 변경은 진단 후 결정(현재로선 불필요 추정).
  - 가설 2: PlaceImage 다중 행 안전성 → 마이그레이션 8d6bab21bc7a에 unique
    constraint 없음, is_representative 단순 boolean. 기존 백로그
    (space-dna-auto-trigger:140 "raw_payload['images']에서 끌어와 PlaceImage로")
    와 본 작업이 일치.
  - 가설 3: 다운스트림 영향 → space_dna_analyzer/oneoff 스크립트 모두
    is_representative.desc()+LIMIT 1 → 다중 행 안전. pins는 Spot.thumbnail_url
    사용으로 PlaceImage 미참조.
  - 가설 4: is_representative=True 중복 우려 → DB 제약 없음, 소비자 LIMIT 1로
    동작상 안전. 본 호출은 첫 장만 True로 신규 회귀 만들지 않음.
- 핵심 변경
  - app/services/spot_creator.py: InstagramData.image_urls: list[str] 추가.
    create_spot_from_naver의 단일 이미지 블록을 리스트 루프로 교체 — 전체 슬라이드
    각각 Supabase Storage 업로드(실패 시 원본 URL 폴백), 첫 장만 is_representative=True,
    Spot.thumbnail_url은 첫 장 영구 URL.
  - app/services/instagram_share.py: _to_instagram_data가 image_urls=list(crawl.images)
    전달. crawl.images는 Apify가 이미 평탄 리스트로 채움.
  - app/schemas/instagram.py: InstagramSaveRequest.image_urls 추가, thumbnail_url은
    폴백용으로 유지(backward compatible).
  - app/routers/instagram.py: save_instagram_spot이 body.image_urls or [thumbnail_url]
    로 합쳐 InstagramData 전달.
- 신규 진단 스크립트 scripts/_oneoff_check_carousel_extraction.py
  - place_raw_data(provider='instagram')의 캐러셀 raw 키 형태 분석
  - images 평탄 길이 vs childPosts/sidecarChildren/carouselMedia 자식 키 존재 여부
  - _normalize_apify 결과 길이와 raw images 길이 비교 → 누락 검출
  - 가설 1 최종 확정 도구. read-only.
- 정책 결정
  - 동기 업로드 (캐러셀 평균 짧음, /share는 RQ 워커 안 실행이라 사용자 응답 무관)
  - SpotResponse.images 노출은 범위 밖 (UI 디자인·페이지네이션 별도)
  - PlaceImage 중복 가드 미추가 (기존 동작 유지)
  - _normalize_apify 자식 키 폴백은 진단 후 결정
- 마이그레이션 없음 (PlaceImage 스키마 그대로 다중 행 지원)
- 알려진 한계 / 후속
  - space_dna_analyzer._pick_image_url은 여전히 1장 사용 → AI 프롬프트 + 분석기
    다중 이미지 활용 작업이 본 적재의 가치를 실제로 끌어냄 (별도 브랜치)
  - 운영 DB 기존 25건 Place 백필 별도 (raw_payload['images'] → PlaceImage 다중 행)
  - SpotResponse.images 노출 + 클라이언트 캐러셀 UI

[2026-05-14 — 공간 DNA 온보딩 POST 엔드포인트 도입]
- 배경: 신규 가입자는 user_space_dna 행이 없어 GET /users/me/space-dna가 항상
  has_data=false. 첫 spot 방문 체크인 후에야 rebuild_user_dna 자동 트리거로
  행이 생김 → 그 사이 추천·피드 화면 콜드 스타트. 프론트엔드 팀이 회원가입 직후
  16개 온보딩 질문으로 3축 비율 사전 수집 기획 확정.
- 신규 엔드포인트
  - POST /users/me/space-dna — 온보딩 16문항 결과(3축 중첩 dict)를 최초 1회 INSERT
    - 응답: 201 Created + UserSpaceDNAResponse (저장한 값 그대로 + total_visits 보존)
    - 409: 이미 mbti_axes 채워진 행 존재 (재호출 차단)
    - 422: 축 키 누락/추가, 유형 키 오류, 값 범위 초과, 두 유형 합≠100 (±0.01 허용)
- 신규 스키마: app/schemas/dna.py
  - REQUIRED_AXES = {"color", "density", "form"} (AI 동결 키 그대로)
  - AXIS_TYPES = {"color": ("high","mild"), "density": ("dense","sparse"),
                  "form": ("fresh","vintage")}  (첫 요소 = AI 단일 값이 가리키는 비율)
  - UserSpaceDNAOnboardingRequest (Pydantic v2 field_validator: 키 strict + 합=100)
- 라우터 수정: app/routers/users.py
  - _normalize_axes_to_pairs 헬퍼: AI 단일 값 → 중첩 dict 변환 (응답 일관성)
    + round(value, 2)로 IEEE 754 차연산 오차 정리
  - GET /users/me/space-dna: 응답 직전에 헬퍼 적용. docstring 3축으로 정정
  - POST /users/me/space-dna: pg_insert + ON CONFLICT DO UPDATE WHERE mbti_axes='{}'
    + RETURNING 단일 SQL로 (a)행 없음→INSERT (b)빈 행→UPDATE (c)채워진 행→409
    세 분기를 원자 처리 (멀티 디바이스 동시 호출 race 안전)
  - total_visits는 UPDATE의 set_에서 의도적 제외 — AI 트리거가 빈 mbti_axes로 행을
    먼저 만들고 visit 카운트만 누적한 시나리오에서 그 값을 보존
- 서비스 수정: app/services/user_dna.py (옵션 2a)
  - _flatten_onboarding_axes 헬퍼: 온보딩 중첩 dict → AI 호환 단일 값 평탄화
    (각 축 첫 유형 비율 추출, AXIS_TYPES 따름)
  - _average_axes 헬퍼: 평균 계산 로직 추출 (단위 테스트 가능)
  - rebuild_user_dna: existing.total_visits==0 → is_first_rebuild → 온보딩
    평탄화 후 valid 풀에 append → spot DNA와 함께 평균
- 정책 결정 (3중 검증)
  - 입력 = 중첩 dict + 합=100 검증: "각 축의 두 반대 유형 합 100" 의미 명확성 우선
  - 영어 키 = 기존 AI 응답 유지: 한글 라벨만 매핑 → AI 마이그레이션·user_dna.py 수정
    모두 불필요 → 작업 범위 최소
  - 재호출 = 409: 온보딩은 1회성. PUT 멱등 의미와 충돌하므로 POST 채택
  - AI 트리거 덮어쓰기 OK → 옵션 2a로 변경 (구현 후 사용자 재검토)
    - rebuild_user_dna에서 첫 rebuild(total_visits==0)에 한해 온보딩 값을
      평탄화(중첩 dict→AI 호환 단일 값)해 평균 풀에 1건 추가
    - 두 번째 visit부터는 자동으로 spot DNA만 평균 (드리프트 없음)
    - total_visits는 spot visit만 카운트 (온보딩은 visit 아님)
  - GET 정규화 옵션 C (백엔드 단일 지점 변환): AI를 중첩 dict로 마이그레이션하는
    옵션 B는 AI팀 합의·평균 로직 수정·마이그레이션 모두 필요해 MVP 일정 위협 →
    백엔드 헬퍼 하나로 단일 값→중첩 dict 변환 + round(value, 2) 적용
- 축 명칭 매핑 (AI팀 확인 2026-05-14)
  - color = 자극 강도 (high ↔ mild)
  - density = 분위기 밀도 (dense ↔ sparse)
  - form = 트렌디함 (fresh ↔ vintage)
- 검증: poetry run python -c "import app.main" PASS, 스키마 validator 단위 검증 (422)
- 마이그레이션 없음 (JSONB 컬럼 구조 변경 불필요)
- 후속
  - 모바일팀 가이드: is_new_user=true 흐름에서만 POST 호출. 백엔드는 409 이중 방어
  - rebuild_user_dna도 중첩 dict로 마이그레이션(옵션 B)은 MVP 이후 검토
  - 단위 테스트는 /tests 디렉토리 부재로 미작성 (인프라 별도)

[2026-05-14 — LLM 기반 인스타 캡션 장소 추출기 도입]
- 배경: 큐레이션 게시물(예: DWtRCqpkZPt — 성북구 카페 5곳)에서 정규식 추출기가
  가게명을 0건 추출. 사용자 화면엔 노이즈 4건(고려대역 6호선·보문사·스타벅스
  한성대입구역점·치코커피)만 표시. 원인은 _PIN_BEFORE_RE가 📍 앞 줄(🚏역명)을
  가게명으로 잡아 실제 가게(❶ 티히커피 줄)를 놓치는 것 + _NON_PLACE_CATEGORY_GROUPS에
  "교통,운수" 누락.
- 신규: app/services/place_extractor_llm.py
  - extract_places(caption, *, hashtags) -> Optional[list[str]]
  - claude-haiku-4-5, MAX_TOKENS=1024, TIMEOUT=15s, TRUNCATE=2000 (disambiguator 통일)
  - 응답 스키마 1필드: ExtractionResult.queries (LLM이 직접 네이버 검색용 query 작성)
  - ANTHROPIC_API_KEY 미설정·예외·파싱 실패 → None → 호출부가 정규식 폴백
  - 시스템 프롬프트: 큐레이션 게시물 N개 추출 / 지하철역·행정구역 제외 / 체인점은
    지점명 포함 / 환각 금지
- 신규 노트: notes/2026-05-14-place-extractor-llm.md (WHY 결정 + 사용자 액션)
- 수정
  - app/services/instagram_share.py
    - _NON_PLACE_CATEGORY_GROUPS에 "교통,운수" 추가 (지하철역 차단, 단독 효과 1건)
    - share_post: LLM 우선 호출 + 정규식 폴백 (used_llm 플래그)
    - LLM이 unique≥2면 disambiguator skip하고 needs_selection 직행
      (큐레이션에서 disambiguator가 임의 1개 자동 저장하는 회귀 방지)
  - scripts/_oneoff_check_instagram_share_extraction.py
    - --with-llm 플래그 추가, 정규식 vs LLM 차집합 비교 출력
    - --with-naver 조합 시 LLM 후보 우선으로 네이버 검색
- 정책 결정 (3중 검증)
  - 응답 스키마 극단 단순화: kind/is_place_post/reason/address 모두 제거 — 후처리 0,
    호출부 단순. 환각 방어는 카테고리 차단·네이버 0건·유니크 1건 3중
  - env flag 추가 안 함 — ANTHROPIC_API_KEY 단일 토글. 비상 차단은 키 제거
  - 빈 결과 LLM 신뢰, 폴백 X — "장소 게시물 아님" 분류 정확도 ↑. 회귀율은 운영
    첫 2주 로그 수집 후 결정
  - 큐레이션 disambiguator skip — used_llm=True AND unique≥2일 때만. 정규식 폴백
    경로는 한 가게 다른 표현 dedupe 약점 때문에 기존 disambiguator 유효 유지
- 검증
  - import 스모크 PASS (place_extractor_llm, instagram_share)
  - DWtRCqpkZPt(성북구 카페 5곳) end-to-end dry-run: LLM이 가게명 5개 모두 추출
    (티히커피·마가밀·카페야벳·아일·카페 기쁜소식 + 성북구), 네이버 1순위가 모두
    음식점·카페 카테고리로 매핑 — 정규식 노이즈(🚏역명 4건 + 도로명 8건)는 LLM
    결과에 0건 누출
- 후속 (범위 밖)
  - 다중 장소 자동 저장 정책 (url 1개당 spot 1개 전제 재설계)
  - LLM 호출 캐싱 (같은 캡션 재호출 비용 절감)
  - 비용 가드 본격화 (월 호출 한도)
  - 캡션 chunking (2000자 초과)
  - LLM 응답 평가 자동화 (golden_set.csv 라벨 추가)

[2026-05-11 — 지도 핀 표시용 엔드포인트 (GET /users/me/pins)]
- 배경: 클라이언트가 네이버 지도 위에 사용자 저장 spot을 핀으로 표시할
  지도 화면이 필요. 기존 GET /storages/{id}/spots는 좌표 미포함 →
  spot마다 GET /places/{id}를 따로 부르면 N+1. 지도 전용 슬림 응답이 필요.
- 신규
  - app/schemas/pin.py: PinResponse(spot_id, storage_id, place_id, name,
    latitude, longitude, is_visited, thumbnail_url)
  - app/routers/users.py에 list_my_pins 핸들러 + _parse_csv_ints/_parse_bbox 헬퍼
  - scripts/_oneoff_check_pins_query.py: PostGIS dry-run (GIST 인덱스 + ST_X/Y/MakeEnvelope)
  - scripts/_oneoff_check_pins.py: 핸들러 직접 호출 검증 (8 케이스)
  - scripts/_oneoff_check_pins_data.py: DB 데이터 진단 (사용자/창고별 spot 분포)
- 변경 없음: 모델/마이그레이션/기존 라우터/app/main.py (users 라우터 이미 등록됨)
- 엔드포인트 명세
  - GET /users/me/pins?storage_ids=1,3,5&visited=true|false&bbox=swLng,swLat,neLng,neLat
  - 다중 storage 합치기 / viewport bbox / visited 필터 모두 한 엔드포인트
  - 응답 1001건까지 받아 truncation 감지 → X-Truncated: true 헤더
  - 비멤버 storage_id 1개라도 있으면 404 + inaccessible_ids
- 정책 결정 (3중 검증)
  - silent drop 대신 404: /storages/{id} 패턴 일관성, 디버깅 명시성
  - 좌표 추출은 SQL ST_X/ST_Y로 raw row 매핑 (PlaceResponse computed_field
    + to_shape는 1000건 시 ORM 메모리 비효율)
  - 핀 클릭 시 인스타 출처는 lazy 로딩 (기존 GET /storages/{id}/spots/{sid} 재사용)
  - bbox 필터는 GIST 인덱스가 자동 사용되므로 zero-cost 안전망
  - 좌표 없는 spot은 응답 제외 (지도에 못 박으니 불필요)
- 운영 Supabase 검증 (읽기만)
  - dry-run: GIST idx_places_coordinate 존재 + ST_X/Y/MakeEnvelope 정상 동작
  - 단일 창고(user 13/storage 13): 21건 정상 반환
  - 다중 창고(user 23/storage 26+27): 3건(2+1) 합쳐서 반환
  - bbox 필터: 한국 전체=21건 / 좁은 영역=0건
  - 비멤버 storage 99999999: 404 + inaccessible_ids
  - 부분 비멤버(26+99999999): 26 데이터 누설 없이 404
  - 형식 오류: storage_ids=abc → 422, bbox=1,2,3 → 422
- 미수행: truncation 케이스(1001건)는 운영 DB 시드 필요로 스킵
- 후속: 클라이언트 팀과 응답 형식·X-Truncated 헤더 합의

[2026-05-11 — 유저 DNA 자동 업데이트 트리거 (MVP P0 마무리)]
- 배경: 조회 API(GET /users/me/space-dna)는 5/10 완료됐지만 쓰기 경로 없어
  신규 가입자가 항상 has_data=false. MVP 4종(인스타·네이버·DNA 조회·유저 DNA 업데이트) 중
  마지막 항목.
- 신규 서비스: app/services/user_dna.py
  - rebuild_user_dna(user_id, db): Spot ⋈ PlaceSpaceDNA 조인 → 4축+confidence
    모든 키 갖춘 행만 평균 → user_space_dna upsert (ON CONFLICT user_id DO UPDATE)
  - record_history_for_spot(user_id, spot_id, db): user_space_dna_history upsert
    (ON CONFLICT spot_id DO UPDATE) — 멱등성 의도의 unique 제약을
    "이 spot의 가장 최근 기여 스냅샷 1행"으로 재해석
  - update_user_dna_after_spot_change(user_id, spot_id): BackgroundTasks 진입점.
    SessionLocal로 새 세션 열기 (요청 세션 재사용 금지 — 요청 종료 시 close됨)
- 라우터 수정: app/routers/spots.py
  - PUT /{spot_id}: prev_visited 캡처 → is_visited 토글(visit↔unvisit) 변화 시
    BackgroundTasks 등록. 메모/평점만 수정 시 재계산 안 함 (가드)
  - DELETE /{spot_id}: visited 스팟 소프트 삭제 시만 트리거
  - 두 핸들러 모두 added_by==current_user.id 조건 — 공유 창고 타인 spot은 미반영
- 정책 결정 (3중 검증)
  - 트리거: BackgroundTasks (Redis 의존 X, DNA 계산은 순수 SQL+Python)
  - 평균: rebuild from scratch (incremental은 unvisit/삭제로 드리프트 누적)
  - 결측치: spot 단위 전체 제외 (4축+confidence 키 모두 있어야 합산)
  - 빈 결과: mbti_axes={} + total_visits=0 update (행 삭제 X) — 조회 API의
    `mbti_axes or None` 정규화로 자연스럽게 has_data=false 응답
  - 사용자 식별: Spot.added_by == current_user (공유 창고 멀티 체크인은 Phase 2)
- 검증 도구: scripts/_oneoff_check_user_dna.py (--user-id 인자, before/after 출력)
- 마이그레이션 없음 (기존 user_space_dna / user_space_dna_history 재사용)
- 스모크 테스트: poetry run python -c "import app.main" PASS
- 운영 Supabase 검증 (읽기+rebuild만)
  - place_space_dna 0행 — AI팀 적재 전이라 평균 계산 자체는 미검증
  - user_id=3에 rebuild 강제 실행 → 0건 합산이라도 빈 행 upsert 안전 확인
  - 회귀 발견·수정: GET /users/me/space-dna, GET /places/{id}/space-dna가
    "행 있음 + mbti_axes={}" 케이스에서 has_data=true 응답하던 문제 → 가드 통일
- 후속: AI팀 DNA 적재 후 visit→unvisit→delete 시나리오 end-to-end 평균 검증
  - 클라이언트가 PUT 호출 후 DNA 즉시 반영 안 될 수 있음(BG 1~2초 지연) — UX 영향 확인

[2026-05-10 — 공간 DNA 조회 API 2종]
- 신규 엔드포인트 2개 (인증 필수)
  - GET /places/{place_id}/space-dna — 장소 DNA 조회. AI팀 미분석 장소는 has_data=false 응답
  - GET /users/me/space-dna — 내 DNA 조회. 신규 유저는 has_data=false + total_visits=0 응답
- 신규 스키마 모듈
  - app/schemas/dna.py: PlaceSpaceDNAResponse, UserSpaceDNAResponse
  - mbti_axes는 dict 그대로 노출 (AI팀 키 변경에 유연)
- 라우터 수정
  - app/routers/places.py: PlaceSpaceDNA import + /{place_id}/space-dna 핸들러
  - app/routers/users.py: UserSpaceDNA import + /me/space-dna 핸들러
- 정책
  - 빈 데이터일 때 404 대신 200 + has_data=false (신규 유저는 항상 빈 상태가 정상이라 404는 옵저버빌리티 노이즈)
  - mbti_axes가 빈 dict({})면 None으로 정규화 (분석 시작 전/완료 후 구분)
  - Place DNA는 인증만 요구 (Place는 글로벌 마스터, 기존 GET /places/{id}와 통일)
- 마이그레이션 없음 (PlaceSpaceDNA / UserSpaceDNA / UserSpaceDNAHistory는 기존 모델 재사용)
- 검증: 라우트 등록 import 테스트 PASS. Swagger UI end-to-end 검증은 사용자 액션 (시드 25건 중 1건 + DNA 없는 1건 양 케이스)
- 후속: 유저 DNA 자동 업데이트(방문 체크인 트리거)가 다음 우선순위

[2026-05-10 — 창고 멤버 초대·관리 API]
- 신규 엔드포인트 6개
  - GET /users/search — 닉네임 prefix 검색 (본인 제외, ILIKE 'q%')
  - POST /storages/{id}/members — user_id로 멤버 추가 (owner)
  - GET /storages/{id}/members — 멤버 목록 (모든 멤버, nickname/profile_image 포함)
  - PATCH /storages/{id}/members/{uid} — role 변경 (owner). role="owner"면 기존 owner는 자동 editor 강등 (atomic, 단일 commit)
  - DELETE /storages/{id}/members/{uid} — 멤버 추방 (owner). 본인 user_id는 400 거부 + /me 안내
  - DELETE /storages/{id}/members/me — 본인 leave (모든 멤버). owner는 400 거부
- 신규 스키마
  - app/schemas/user.py: UserSearchResponse (id, nickname, profile_image — email 노출 안 함)
  - app/schemas/storage.py: StorageMemberAddRequest(user_id, role: editor|viewer),
    StorageMemberRoleUpdate(role: owner|editor|viewer),
    StorageMemberDetailResponse(member 필드 + nickname + profile_image)
- 라우터 헬퍼
  - app/routers/storages.py: _get_target_member, _to_member_detail
  - 기존 _get_member 그대로 재사용 (required_roles=("owner",))
- 정책
  - 이메일 검색이 아닌 nickname prefix 검색을 채택 — 카카오 미동의 사용자가
    임시 이메일(kakao_{id}@picklog.local)이라 친구가 알 수 없는 문제 우회
  - owner 1명 단일 모델 유지 (transfer 시 기존 owner는 editor로 강등)
  - email은 응답에 노출 안 함 (privacy)
  - /members/me 라우트를 /members/{user_id}보다 먼저 등록 (FastAPI 라우트 매칭 순서)
- 마이그레이션 없음 (기존 storage_members 테이블 그대로 사용)
- 로컬 검증: 13개 시나리오 PASS (닉네임 검색·멤버 추가/추방·소유권 이전 atomic·self-leave 등)


[2026-05-08 — 인스타 raw → place_raw_data 1차 소스 통합]
- 배경: 인스타 raw 데이터가 instagram_post_cache(전체 payload)와 place_raw_data(축약본)
  두 테이블에 분산돼 있었고, caption은 정제 테이블(spots)에 노출되지 않아 사용자에게
  보여줄 수 없었음. "raw → 정제(places/spots) → 사용자 표시"의 1차 소스 일원화 목표.
- 마이그레이션 (Alembic: d4f9a1b3c5e7)
  - place_raw_data.place_id NOT NULL → nullable (raw 먼저 적재, 정제 단계에서 UPDATE)
  - spots.caption TEXT NULL 컬럼 추가 (응답 SpotResponse에 노출)
  - 기존 축약본 행(provider_place_id=NULL)에서 raw_payload->>'url'로 shortcode 정정
    (regexp_match로 capture group 추출, 단일 후보 NOT EXISTS 가드)
  - instagram_post_cache → place_raw_data 데이터 복사
    (메타키 _source/_url을 raw_payload에 주입, ON CONFLICT 시 place_id 보존)
  - 기존 spots에 caption 백필 (raw_payload->>'caption' DISTINCT ON)
  - instagram_post_cache 테이블 DROP
- 코드 변경
  - app/models/models.py: PlaceRawData.place_id Optional, Spot.caption 컬럼 추가,
    InstagramPostCache 클래스 제거, Place.raw_data cascade='save-update, merge'로 좁힘
  - app/services/instagram_pipeline.py: CachedPost dataclass 도입,
    get_cached/save_cache가 place_raw_data 사용, restricted TTL은 collected_at 기준
  - app/services/spot_creator.py: InstagramData에 shortcode 추가, 인스타 raw INSERT를
    조건부 UPDATE(WHERE place_id IS NULL)로 교체, Spot 생성 시 caption 채움
  - app/services/instagram_share.py: _to_instagram_data에서 shortcode 채움
  - app/schemas/spot.py: SpotResponse에 caption 추가
- 안전장치
  - ON CONFLICT set_에 place_id 미포함 → 정제 연결 보존
  - collected_at=NOW() 갱신 → restricted TTL 정상 작동
  - 조건부 UPDATE → 동시성 race 방지
  - delete-orphan 제거 → ORM 컬렉션 조작이 raw 자동 삭제 안 함
- 영향
  - 사용자 화면(SpotResponse)에 caption이 직접 노출됨 (raw_payload JSONB 파싱 불필요)
  - 인스타 1차 raw가 단일 테이블(place_raw_data)에 통합 — Apify 응답 정보 손실 없음

[2026-05-01]
- OAuth2 인증 방식 수정: JSON body -> OAuth2PasswordRequestForm (Swagger 호환)
- 리스트 API 페이징 적용: storages, spots에 page/size 파라미터 추가
- Places 검색 API 신규 추가: GET /places?q=, GET /places/{id}
- Instagram 예외 처리 강화: 비공개/삭제 게시물 감지 (404)
- UserResponse에서 kakao_id 필드 제거 (미구현 기능 노출 방지)
- python-multipart 패키지 추가

[2026-05-02]
- GET /places/{id}/raw-data 엔드포인트 추가: place_raw_data 원천 데이터 조회
- place_reviews 테이블 신설 (Alembic migration: 5fe6c16c6978)
- PlaceResponse coordinate → latitude/longitude 분리 필드로 개선
- 네이버 지도 기반 장소 Upsert 초기 구현 (migration: a3f8b2c1d947)
  - place_raw_data partial unique index: (provider, provider_place_id) WHERE provider_place_id IS NOT NULL
  - Instagram 공유 → 기본 저장소 자동 저장 기능

[2026-05-03]
- 네이버 지도 장소 Upsert 완성 (POST /places/from-naver)
  - app/schemas/place.py: NaverPlaceUpsertRequest, NaverPlaceUpsertResponse 추가
  - app/routers/places.py: POST /places/from-naver 엔드포인트 (WKTElement 좌표 변환, IntegrityError 충돌 처리)
  - 응답: { place_id, created: bool, place }
    - created=true: 신규 생성 / created=false: 기존 재사용

- Instagram 파이프라인 정비 (방식 C 적용) — 내부 재크롤링 제거
  - app/schemas/instagram.py: InstagramSaveRequest 완전 교체
    - 구: url + storage_id
    - 신: instagram_url + caption + thumbnail_url + naver_place_id(필수) + place_name + 좌표 + storage_id 등
  - app/routers/instagram.py: save_instagram_spot 로직 교체
    - async → def (Playwright 의존성 제거)
    - naver_place_id 기준 Place upsert (IntegrityError → rollback → 재조회 패턴)
    - already_saved: 같은 storage에 동일 Place Spot 존재 시 기존 Spot 반환
    - Instagram PlaceRawData + PlaceImage 저장 유지
  - 제거: "이름 없음" Place 자동 생성, Instagram location_id 기반 중복 감지(비인증 한계)

- Instagram 장소 중복 방지 (location_id 기반 upsert) 구현
  - app/services/instagram_crawler.py: _extract_location_from_scripts() 추가
    - <script> 태그 JSON에서 "location":{"id":"...","name":"..."} 패턴 탐색
    - 실패 시 기존 OG description 파싱으로 fallback
  - app/schemas/instagram.py: InstagramCrawlResponse에 instagram_location_id 필드 추가
  - app/schemas/instagram.py: InstagramSaveResponse 신규 추가 (spot, already_saved, place_created)
  - app/routers/instagram.py: POST /instagram/save 응답 모델 변경 및 upsert 로직 적용
    - already_saved=true (HTTP 200): 이미 같은 장소 저장됨 → 기존 Spot 반환
    - already_saved=false (HTTP 201): 신규 저장
    - place_created: 새 Place 생성 여부

- 주의사항:
  - Instagram location_id는 비인증 상태에서 추출 불가 (Instagram이 스크립트 JSON 숨김)
    → 현재 provider_place_id=None으로 저장, location_id 기반 중복 방지는 미작동
    → 향후 쿠키 인증 또는 네이버 API 우회 방식으로 해결 필요
  - Playwright + 핫리로드 병행 시 greenlet 오류 발생
    → 코드 수정 후 반드시 서버 완전 재시작 필요
    → 로컬 테스트는 `poetry run uvicorn app.main:app` (핫리로드 없이) 권장

- 카카오 OAuth 로그인 추가 (POST /auth/kakao)
  - 모바일 카카오 SDK가 발급한 access_token을 받아 자체 JWT 발급
  - app/services/kakao_oauth.py: 신규, fetch_kakao_user() — httpx로 v2/user/me 호출
  - app/schemas/user.py: KakaoLoginRequest, KakaoLoginResponse 추가
  - app/routers/auth.py: login_kakao() 핸들러 추가
  - 매칭 우선순위: kakao_id → email (병합) → 신규 가입
  - 신규 가입 시 기본 저장소("내 저장소") + owner 멤버 자동 생성 (register와 동일 패턴)
  - 이메일 동의 X 시 kakao_{id}@picklog.local 형태로 임시 이메일 자동 생성
  - 응답에 is_new_user 플래그 포함 (모바일 온보딩 플로우 지원)
  - 의존성: httpx (>=0.28.0,<0.29.0) 추가
  - 환경변수: KAKAO_REST_API_KEY (.env.example 기재, 현재 미사용)

[2026-05-07 — Phase 2: /share 비동기화 + 비용 가드 통합]
- POST /instagram/share를 하이브리드 sync/async로 변경
  - 캐시 hit: 즉시 share_post 동기 실행 → status="done", result=InstagramShareResponse
  - 캐시 miss: instagram_crawl_jobs 행 생성 + RQ enqueue → status="pending", job_id 반환
  - /crawl-async와 동일한 enqueue 응답 패턴 (InstagramShareEnqueueResponse)
  - 응답 모델 변경: 기존 InstagramShareResponse 직접 반환 → InstagramShareEnqueueResponse
    (기존 클라이언트는 status="done" 분기에서 result 필드 안의 InstagramShareResponse를 꺼내 사용)
- GET /instagram/share-jobs/{job_id} 신규 (share 잡 폴링)
  - kind='share' 필터로 crawl 잡과 분리
  - 잡 등록자(user_id)만 조회 가능 (다른 사용자 잡은 404로 가림)
- 기존 /jobs/{id}는 kind='crawl' 필터 추가해 crawl 잡만 응답
- instagram_crawl_jobs 스키마 확장 (Alembic: c8d3e5f1a934)
  - kind: 'crawl' | 'share' (server_default='crawl', NOT NULL)
  - user_id, storage_id (share 잡 한정 nullable, FK with ondelete=SET NULL)
  - 인덱스 추가: ix_instagram_crawl_jobs_kind_created
- 워커 함수 신규: app/services/instagram_jobs.process_share_job
  - 잡 행에서 user_id/storage_id 로드 → User 조회 → share_post 호출
  - 결과를 share_result_to_response로 직렬화해 payload에 저장
  - 도메인 예외(권한·중복·네이버 실패 등)는 status="failed" + error로 보고
- 비용 가드(#10) 자동 통합
  - share 잡도 source='apify'로 마킹되므로 _is_apify_budget_exceeded가 자동 카운트
  - 별도 코드 변경 없음, 잡 카운트 한 번에 share + crawl-async 통합

- 잡 dedupe는 일단 도입 안 함 (Spot의 unique constraint로 결과적 보호)
- 인증 컨텍스트는 user_id만 직렬화 (JWT 토큰 저장 X, 워커가 DB에서 User 재로딩)

[2026-05-07 — Phase 1: /share 신뢰성 보강]
- 네이버 Local Search 실패가 not_a_place_post로 위장되던 문제 수정
  - app/services/naver_local_search.py: 키 미설정·네트워크 오류·non-200·JSON 파싱 실패를
    빈 리스트 swallow 대신 NaverLocalSearchError로 raise
  - app/routers/instagram.py: share_instagram_post에서 502 Bad Gateway로 매핑
  - 정책: 일부 후보만 성공한 결과로 자동 매핑하면 못 본 후보 매칭을 빠뜨려 오탐 위험.
    외부 의존성 실패는 빈 결과로 묻지 않고 클라이언트에 명확히 알림(전체 거절).
- instagram_post_cache의 restricted_page 응답이 영구 hit되던 문제 수정
  - app/services/instagram_pipeline.py:
    - _RESTRICTED_TTL_SECONDS = 6 * 3600 상수
    - _is_restricted_expired 헬퍼: source='apify' + payload.error='restricted_page' + age>=TTL
    - get_cached: 만료된 restricted 캐시는 None 반환 → fetch_post 자연 재크롤
    - save_cache: PostgreSQL ON CONFLICT DO UPDATE로 변환(fetched_at NOW() 갱신)
      → 만료 후 재크롤 시 INSERT만으로는 PK 충돌해 빈약한 기존 행이 남는 문제 해소
  - 정상 캐시(완전한 caption/위치)는 TTL 없음 — 인스타 캡션 편집 빈도 낮아 비용 추가만 발생
- 마이그레이션 없음 (스키마 무변동, fetched_at 컬럼은 기존 그대로)

[2026-05-07]
- 인스타 공유 자동 장소 매핑 + 저장 도입 (방식 D — POST /instagram/share)
  - 배경: Apify가 restricted 응답이라도 본문 + 임베디드 주소(📍...)는 안정 추출 → 방식 C(수동 선택 전제)의 데이터 부족 전제가 무너짐
  - 흐름: fetch_post → 캡션 후보 추출 → 네이버 Local Search → naver_place_id dedupe
    - 유니크 1건이면 자동 저장 (status="saved")
    - 0건 또는 다수면 후보 + crawl_data 반환 (status="needs_selection") → 클라이언트가 사용자 선택 후 기존 /instagram/save 호출
  - 신규 서비스
    - app/services/naver_local_search.py — Naver Local Search Open API 래퍼 + mapx/mapy(WGS84×10⁷) 좌표 정규화
    - app/services/place_extractor.py — 캡션에서 📍/주소 패턴/첫 줄 구분자 기반 후보 추출
    - app/services/spot_creator.py — Place upsert + Spot 생성 로직 추출 (NaverPlaceData/InstagramData 데이터클래스, 커스텀 예외)
    - app/services/instagram_share.py — 오케스트레이션
  - 수정
    - app/services/instagram_pipeline.py: _normalize_apify에 restricted_page 응답(description/image)→(caption/images[0]) 매핑 추가
    - app/routers/instagram.py: /save를 spot_creator에 위임 + /share 핸들러 추가
    - app/schemas/instagram.py: InstagramShareRequest/Response, PlaceCandidate, InstagramCrawlData 추가
  - 환경변수: NAVER_CLIENT_ID, NAVER_CLIENT_SECRET (이미 .env.example 정의됨, 신규 사용처)
  - 정책
    - 자동 저장은 후보가 1건으로 수렴할 때만 (오탐 방지)
    - 다중 장소 게시물(큐레이션) 자동 처리는 포기, 수동 폴백
    - LLM 추출 미사용 (MVP 비용·지연 부담)

[2026-05-06]
- Apify 기반 인스타그램 크롤링 파이프라인 도입
  - 기존 비로그인 OG 크롤러는 위치 태그 없는 게시물 본문 추출 불가 → Apify 액터(`apify/instagram-post-scraper`)를 기본 크롤러로 채택, OG는 fallback
  - 신규 테이블 2개 (Alembic: b7c4d2e9a812)
    - instagram_post_cache (shortcode PK, payload JSONB, source 'apify'|'og_fallback')
    - instagram_crawl_jobs (id UUID, status, source, payload, error)
  - 신규 서비스
    - app/services/apify_client.py — ApifyClient.actor.call() 래핑, 예외 정의
    - app/services/instagram_pipeline.py — shortcode 추출, 캐시 조회, Apify→OG fallback, 정규화, 월 비용 가드
    - app/services/instagram_jobs.py — RQ 잡 함수 process_crawl_job(job_id)
    - app/worker.py — RQ 워커 엔트리포인트 (Windows 호환 SimpleWorker)
  - 신규 엔드포인트
    - POST /instagram/crawl-async — 캐시 hit 즉시 반환, miss면 job_id 발급+enqueue
    - GET  /instagram/jobs/{job_id} — 잡 폴링
  - 기존 엔드포인트 유지 (변경 없음)
    - POST /instagram/crawl (동기 OG)
    - POST /instagram/save (place_raw_data/place_images에 Apify 응답 그대로 저장)
  - 인프라
    - app/main.py: Redis 연결 + RQ 큐를 lifespan에서 초기화 (Redis 미가동이면 라우터 503으로 graceful degrade)
    - docker-compose.yml: worker 서비스 추가 (profile=worker, 별도 실행)
  - 의존성: apify-client (>=1.8.0), rq (>=2.0.0)
  - 환경변수: APIFY_TOKEN, APIFY_INSTAGRAM_ACTOR_ID, REDIS_URL, APIFY_MONTHLY_BUDGET_USD
  - 비용 가드: 이번 달 source='apify' 잡 카운트 × 추정 단가가 한도 넘으면 즉시 OG fallback

[2026-05-08]
- 네이버 블로그 enrichment(RQ 잡 기반 본문 수집) 도입 — AI 학습 코퍼스 확보용
  - 트리거: /instagram/share 자동 저장 + /instagram/save 수동 저장 모두 (best-effort enqueue)
  - 새 잡 종류: kind="naver_blog_fetch" (InstagramCrawlJob 재사용, 마이그레이션 없음)
  - 흐름: place_id 잡 → 30일 freshness 체크 → quota 가드 → 검색 API → 각 link 모바일 페이지 본문 fetch → place_raw_data + place_reviews 적재
  - 신규 서비스 4개
    - app/services/naver_blog_search.py — Blog Search API 래퍼 (Local 패턴 미러)
    - app/services/naver_blog_body_fetcher.py — m.blog.naver.com 본문 크롤러 (httpx + BS, 모바일 UA, SmartEditor 3 셀렉터 우선, 광고/협찬 마커 검출)
    - app/services/place_enrichment.py — enqueue + freshness/quota + 검색→본문→DB 적재 오케스트레이션
    - app/services/queue.py — 워커 안에서 RQ Queue 핸들 얻는 헬퍼
  - 수정
    - app/services/instagram_jobs.py: process_blog_fetch_job 신규, process_share_job 끝부분에 enqueue 추가
    - app/routers/instagram.py: save_instagram_spot에 Request 파라미터 + enqueue 추가
  - 정책
    - 30일 갱신 (place_raw_data.collected_at 기준, 그 이내면 skip)
    - 본문 우선, 실패 시 스니펫 fallback (PlaceReview.text)
    - 본문 fetch 실패율 50% 초과 시 잡을 'blocked'로 마킹 (다음 잡에서 재시도)
    - 쿼터 카운트는 payload에 'inserted_reviews' 키가 있는 행만 (스킵 잡 제외)
    - 한국 주소 시/도 접미사 토큰 제거 후 검색 (매칭률 향상)
  - 환경변수: NAVER_BLOG_MONTHLY_QUOTA_CALLS, NAVER_BLOG_BODY_FETCH_SLEEP(0.5), NAVER_BLOG_BODY_MAX_CHARS(2000)
  - 검증: import OK, 검색 API 3건 반환, 모바일 페이지 본문 1833자 추출(SmartEditor 3), ON CONFLICT partial index SQL OK
  - 기존 /places/from-naver의 BackgroundTasks 기반 naver_blog.py와 공존 (통합은 별도 작업으로 유보)

[2026-05-08 — AI팀용 운영 Places 시딩 도구 도입]
- 목적: AI팀이 공간 DNA(MBTI 4축) 알고리즘 개발·검증할 수 있게 운영 Supabase 채워넣기
- 신규
  - scripts/seed_places.py — 시드 계정 운영 API 호출 스크립트 (httpx 동기)
    - --mode share: seeds/instagram_urls.txt → POST /instagram/share (cache hit 동기 / miss 시 share-jobs/{id} 폴링)
    - --mode naver: seeds/places_naver_fallback.csv → POST /places/from-naver
    - 401 자동 재로그인, 502/503/타임아웃 지수 백오프 재시도
    - --limit N으로 dry-run 표본 측정 가능 (saved 비율 → 메인 경로 결정)
  - seeds/instagram_urls.txt — 인스타 URL 입력 양식 (DNA 4축 양극단 분포 가이드)
  - seeds/places_naver_fallback.csv — 네이버 메타 폴백 양식
  - seeds/golden_set.csv — AI팀 검증용 사전 라벨링 10건 양식
  - seeds/README.md — 사전 셋업/실행/검증/AI팀 인터페이스 가이드
  - notes/2026-05-08-ai-places-seeding.md — 작업 메모 (WHY 포함)
- 사전 셋업 (사용자 액션 필요)
  - Railway에 RQ 워커 프로세스 떠 있는지 확인 (HIGH 차단성)
  - Railway 환경변수: APIFY_TOKEN, APIFY_INSTAGRAM_ACTOR_ID, NAVER_CLIENT_ID/SECRET, REDIS_URL
  - test@example.com 운영 계정 등록 + 비밀번호 설정 (POST /auth/register, owner Storage 자동 생성)
  - seeds/instagram_urls.txt에 100건 큐레이션 (4축 양극단 균등)
- 다음 단계
  - dry-run 20~30건 → saved 비율 측정 후 메인 경로 결정
  - saved≥50% → 인스타 메인 유지 / saved<50% → 네이버 직입력 경로로 격하
  - AI팀 합의 후 place_space_dna mbti_axes 키 명세 동결 (plan v3 권장: busy_calm/calm_flashy/modern_vintage/premium_value, -1.0~1.0)
- 참고: ~/.claude/plans/ai-delightful-marshmallow.md (검증 보강 v3)

[2026-05-14 — 앱 내 네이버지도 저장 → IG 동등 사이드이펙트 지원]
- 목적: 기존 POST /places/from-naver는 Place upsert만 해서 사진/리뷰/공간DNA가 비어있던 격차 해소.
  앱에서 네이버지도로 검색해 저장해도 IG 흐름과 동일한 데이터 품질(사진+리뷰+DNA)이 나오게 함.
- 신규 엔드포인트: POST /storages/{storage_id}/spots/from-naver (NaverSpotCreateRequest / NaverSpotCreateResponse)
- 사이드이펙트 4종 (IG 흐름과 동등):
  1. Place upsert (naver_place_id 기준) — 기존 _upsert_naver_place 재사용
  2. PlaceImage 저장 (image_url 제공 시 Supabase Storage 업로드, source='naver', uploaded_by=current_user.id)
  3. Spot 생성 (instagram_url=None, caption=None, user_memo/user_rating 채워짐)
  4. 후속 RQ enqueue 2종 (best-effort): enqueue_blog_fetch_job + enqueue_space_dna_analysis
     - 두 워커 모두 멱등 가드 내장 (_should_refresh 30일 / _already_analyzed) → already_saved 케이스에도 안전
- 신규 파일
  - app/dependencies/queue.py — get_rq_queue 공용 헬퍼 (IG·네이버 라우터 공유)
- 수정
  - app/services/image_storage.py — _upload_to_supabase 코어 일반화, namespace 인자 도입, upload_naver_place_image 추가, upload_instagram_image는 wrapper로 유지(시그니처 동일)
  - app/services/spot_creator.py — create_spot_from_naver_manual 신규 (IG-specific 로직 없이 매뉴얼 저장 전용)
  - app/schemas/spot.py — NaverSpotCreateRequest / NaverSpotCreateResponse 추가
  - app/routers/storages.py — 새 핸들러 + place_enrichment / space_dna_analyzer / spot_creator import
  - app/routers/instagram.py — _get_rq_queue 제거, app/dependencies/queue.get_rq_queue import로 일원화
- 보존
  - 기존 POST /places/from-naver는 그대로 (장소만 upsert 의미 유지, 기존 클라이언트 호환)
- 알려진 한계 / 본 PR 범위 외
  - uq_spots_storage_place는 partial 아님 → soft-deleted Spot 있으면 같은 (storage, place) 재저장 시 IntegrityError (IG도 동일 이슈)
  - 이미지 없이 저장된 spot은 향후 사진 추가 시 DNA 재트리거 미구현
- 검증
  - 앱 부트스트랩 import OK, /storages/{storage_id}/spots/from-naver 라우트 등록 OK
  - _build_storage_path('naver', '12345678', ...) → 'naver/12345678/foo.png' 정상
  - 회귀: 기존 /places/from-naver, /instagram/save, /instagram/share 라우트 동일 등록

============================================================
